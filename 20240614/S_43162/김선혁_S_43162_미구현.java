class Solution {
    public int solution(int n, int[][] computers) {
        int answer = 0;
        
        // 시작점을 기준으로 하나의 부모노드에 체크를하고
        // 전체 부모노드 한번씩 돌아서 
        // 전체 스캔해서 개수 count
        int[] parent = new int[computers.length];
        for(int i = 0;i)
        
        
        
        return answer;
    }
}
